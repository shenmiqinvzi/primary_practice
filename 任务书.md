# 苍穹外卖项目 · 任务书

> 本文档是「苍穹外卖」(Sky Take-Out) 项目的**进度说明 + 总体设计框架 + 交接指南**。
> 目的是让接手方（其他 AI 或开发者）能无缝接手，继续完成剩余功能，并写出与现有代码风格完全一致的代码。
> 项目性质：**教学项目**，按黑马程序员《苍穹外卖》课程「边讲边做」推进。**接手后请继续保持教学节奏，见文末「九、对接手方的工作要求」。**

---

## 一、项目概述

| 项 | 内容 |
|---|---|
| 项目名 | 苍穹外卖（Sky Take-Out） |
| 定位 | 外卖点餐系统，管理端（后台）+ 用户端（小程序/Web） |
| 课程 | 黑马程序员《苍穹外卖》 |
| 当前进度 | 管理端核心模块已全部完成并测试通过；用户端刚起步（仅店铺状态查询） |

### 已实现的功能全景

```
管理端 /admin（需要员工 JWT）
├── 员工管理    登录 / 分页 / 新增 / 编辑 / 启用停用
├── 分类管理    分页 / 新增 / 修改 / 删除 / 启用停用 / 按类型查列表
├── 菜品管理    分页 / 新增(含口味) / 批量删除 / 修改 / 启用停用 / 按分类查列表
├── 套餐管理    分页 / 新增(含关联菜品) / 批量删除 / 修改 / 启用停用 / 回显
├── 通用上传    OSS 图片上传
└── 店铺营业状态  设置 / 查询

用户端 /user（暂不校验 token）
└── 店铺营业状态  查询
```

---

## 二、开发环境（必须与本机一致）

| 组件 | 版本 / 位置 |
|---|---|
| JDK | **Java 17**（Microsoft OpenJDK，路径 `C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot`） |
| Maven | 3.9.16（`E:\maven-3.9.16-bin\apache-maven-3.9.16\bin\mvn`，项目**没有 mvnw**） |
| MySQL | 8.0.46，本地 `localhost:3306`，库名 `sky_take_out`，账号 root |
| Redis | **Docker Desktop 容器**，`localhost:6379`，无密码。容器名 `sky-redis`（镜像 redis:7） |
| 阿里云 OSS | bucket：`heicongqiong`，endpoint：`oss-cn-beijing.aliyuncs.com` |
| 接口文档 | Knife4j：`http://localhost:8080/doc.html`（管理端接口需先登录拿 token 填到头部 `token`） |
| 服务端口 | 8080 |

### 常用命令（Windows 下在 sky-server 模块目录执行）

```bash
# 打包（sky-pojo 改动后必须先 install，否则依赖模块是旧的）
mvn clean install -DskipTests
# 运行
cd sky-server && mvn spring-boot:run
# Redis 操作
docker exec sky-redis redis-cli get SHOP_STATUS
docker exec -it sky-redis redis-cli
```

> ⚠️ `mvn spring-boot:run` 必须在 **sky-server** 模块下执行，在根目录会报 "Unable to find a suitable main class"。
> ⚠️ 8080 端口可能被用户 IDE 里手动跑的实例占用，先确认。

---

## 三、项目结构（Maven 多模块）

```
苍穹外卖/
├── pom.xml                  # 父 POM（统一版本管理）
├── sky-common/              # 通用模块：结果封装、异常、常量、工具、上下文
│   └── src/main/java/com/sky/
│       ├── constant/        #   StatusConstant / JwtClaimsConstant / ShopConstant
│       ├── context/         #   BaseContext（ThreadLocal 存当前登录用户 ID）
│       ├── exception/       #   BaseException / AccountNotFoundException / ...
│       ├── result/          #   Result<T> / PageResult
│       └── utils/           #   JwtUtil
├── sky-pojo/                # 数据载体模块：entity / dto / vo
│   └── src/main/java/com/sky/
│       ├── entity/          #   Employee / Category / Dish / DishFlavor / Setmeal / SetmealDish
│       ├── dto/             #   各模块的请求参数对象
│       └── vo/              #   各模块的响应对象
├── sky-server/              # 应用模块：Controller / Service / Mapper / 配置 / 拦截器
│   └── src/main/
│       ├── java/com/sky/
│       │   ├── controller/  #   admin/ 与 user/ 两个子包
│       │   ├── service/     #   接口
│       │   ├── service/impl/#   实现
│       │   ├── mapper/      #   MyBatis 接口（注解 SQL 或 XML）
│       │   ├── config/      #   WebMvcConfiguration / OSSConfiguration / JacksonConfig
│       │   ├── interceptor/ #   JwtTokenAdminInterceptor（仅此一个，拦 /admin/**）
│       │   ├── handler/     #   GlobalExceptionHandler
│       │   ├── properties/  #   JwtProperties / OssProperties
│       │   └── utils/       #   OssUtil
│       └── resources/
│           ├── application.yml
│           └── mapper/      #   *Mapper.xml（MyBatis 动态 SQL）
└── doc/sql/sky_take_out.sql # 建表脚本（当前仅 6 张表，后续需补，见第七节）
```

**依赖方向（单向）**：`sky-server → sky-pojo / sky-common`，pojo 与 common 之间不互相依赖。

---

## 四、核心架构与约定（接手方必须逐条遵守）

后续所有新代码，**必须与现有模式完全一致**。这是让项目保持统一的底线。

### 1. 三层架构
`Controller → Service（接口 + Impl）→ Mapper（接口 + XML）`。Controller 不写业务逻辑，Service 不出现 SQL，Mapper 只负责数据访问。

### 2. 统一返回 `Result<T>`
所有接口返回 `Result<T>`（sky-common），形如 `{code: 1, msg: null, data: ...}`。成功 `Result.success(data)`，失败由异常处理器接管，Controller **不要自己 try-catch 业务异常**。

### 3. 全局异常处理（GlobalExceptionHandler）
- `BaseException` 及其子类 → 业务错误，返回 `Result.error(msg)`，msg 就是异常消息
- 其他 `Exception` → 500 "服务器内部错误"
- 新增业务校验失败时：**throw new BaseException("提示语")**，不要返回 null

### 4. JWT 登录鉴权
- 管理端 `JwtTokenAdminInterceptor` 拦截 `/admin/**`，放行 `/admin/employee/login`；请求头 `token` 携带 JWT
- 校验通过后 `BaseContext.setCurrentId(员工id)`，业务代码里用 `BaseContext.getCurrentId()` 取当前操作人
- JWT 配置在 `application.yml` 的 `sky.jwt.admin-*`，`JwtProperties` 读取

### 5. 审计字段
`create_time / create_user` 创建时赋值，之后不再改；`update_time / update_user` 每次更新都刷新。新增时四个字段都写 `LocalDateTime.now()` + `BaseContext.getCurrentId()`。

### 6. 一对多关联「先删后插」重建
修改菜品/套餐时：先 `update` 主表 → `deleteByXxxId` 删关联表 → 重新 `insertBatch` 插入。新增时：先 `insert` 主表（`useGeneratedKeys="true" keyProperty="id"` 回填自增 id）→ 给子实体逐个 set 主表 id → `insertBatch`。**空列表判断：`if(list!=null && !list.isEmpty())`，insertBatch 必须放在 if 内部**。

### 7. 分页
`PageHelper.startPage(page, pageSize)` + Mapper 返回 `Page`，转 `PageResult(total, records)`。Mapper 方法签名返回 `List<XxxVO>`（强转 `Page<XxxVO>`）。

### 8. 依赖注入
统一 `@Autowired` 字段注入。构造器注入用于 @ConfigurationProperties 组件（OssUtil 用 @AllArgsConstructor）。

### 9. 常量
业务状态用常量类，如 `StatusConstant.ENABLE(1)` / `DISABLE(0)`；Redis key 用 `ShopConstant.SHOP_STATUS`。**不要写魔法数字**。

### 10. DTO / VO / Entity 职责
- **Entity**：和数据库表一一对应，纯字段 + Lombok 注解（@Data @Builder @AllArgsConstructor @NoArgsConstructor implements Serializable）
- **DTO**：接收请求参数（新增/修改用同一个 DTO，**新增不传 id，修改必须传 id**）
- **VO**：返回给前端、可能含多表字段（如 `DishVO` = dish 字段 + flavors 列表 + categoryName）
- 属性拷贝用 `BeanUtils.copyProperties`，List 里的对象要逐个 set 关联外键

### 11. 命名与包
- Java 公共类名必须与文件名**完全一致**（含大小写，如 `OSSConfiguration`）
- Mapper XML 的 `resultType`：实体类可写短名（type-aliases 只配了 `com.sky.entity`），**VO 必须写全限定名 `com.sky.vo.XxxVO`**
- SQL 表名**一律小写**
- 枚举状态用常量类；Controller 加 `@Api(tags="...")`，方法加 `@ApiOperation("...")`

### 12. Lombok
@Slf4j 打日志；@Data 生成 getter/setter。**新增了字段，记得实体类要配齐 Lombok 注解**（否则 MyBatis 无法反射赋值）。

---

## 五、已踩过的坑（接手方先看，避免重蹈覆辙）

这些是开发过程中实际踩过并解决的坑，接手后遇到同类问题可对照。

1. **`OSSConfiguration.java` 文件名与类名大小写不一致** → 整个 sky-server 模块 Lombok 失效、大量报错。改名即可。
2. **sky-pojo 改动后没 `mvn clean install`** → 运行报 `NoClassDefFoundError`。pojo/common 改了必须重新 install。
3. **YAML 冒号后必须有空格**（`key: value`），否则键读不到。曾导致 OSS AccessKey 为空报错。
4. **Spring Boot 2.7 的 Redis 配置前缀是 `spring.redis`**，不是 3.x 的 `spring.data.redis`。
5. **MyBatis XML 里 `resultType` 写成不存在的类（如 `com.sky.entity.SetmealVO`）** → 启动时解析所有 XML，ClassNotFoundException，整个应用起不来。VO 类一律全限定名。
6. **联表查询列歧义**：`from setmeal s left join category c` 后，`name` / `create_time` 两表都有 → 必须加表别名前缀 `s.name`、`s.create_time`。
7. **`<if test>` 里写表别名（`test="s.name"`）会 OGNL 报错**：`test` 表达式作用在 Java 参数对象上，只能写参数属性名（`name`）；表别名只属于 SQL 文本那半边。
8. **两个包下同名 Controller（`ShopController`）** → bean 名都是 `shopController`，Spring Boot 默认不允许覆盖，启动崩。解决：类名不同（本项目已用 `AdminShopController` / `UserShopController`）。
9. **新增/修改共用 DTO，修改不传 id** → `update where id=null` 更新 0 行，一路 null 到最后撞数据库非空约束。测试修改接口必须带 `id`。
10. **判空位置**：`deleteBatch` 开头要判 `ids==null||ids.isEmpty()`；`insertBatch` 要放在 `if` 里。
11. **Knife4j 3.0.3 不显示 MultipartFile 参数** → 测文件上传用 `curl.exe`（PowerShell 的 `curl` 是 Invoke-WebRequest 别名，需 `curl.exe -F "file=@路径"`）。
12. **javac 编译参数**：方法参数名大小写会影响 `@Select` 注解里 `#{参数名}` 的匹配，参数名统一小写驼峰。

---

## 六、数据库现状

`doc/sql/sky_take_out.sql` 目前有 **6 张表**：

| 表 | 用途 | 关键字段 |
|---|---|---|
| `employee` | 员工 | id, name, username, password(MD5), status, 审计字段 |
| `category` | 分类 | id, type(1菜品/2套餐), name(唯一), sort, status |
| `dish` | 菜品 | id, category_id, name, price, image, description, status |
| `dish_flavor` | 菜品口味 | id, dish_id, name, value |
| `setmeal` | 套餐 | id, category_id, name, price, image, description, status |
| `setmeal_dish` | 套餐-菜品关联 | id, setmeal_id, dish_id, name, price, copies；`UNIQUE(setmeal_id, dish_id)` |

> ⚠️ **后续模块需要新增以下表（黑马标准）**，接手方需在 SQL 文件中补建：`user`、`address_book`、`shopping_cart`、`orders`、`order_detail`。字段设计见第八节各模块说明。

---

## 七、关键配置（application.yml 现状）

```yaml
server:
  port: 8080
spring:
  redis:                     # ⚠️ 2.7 前缀是 spring.redis
    host: localhost
    port: 6379
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher   # 必须！否则 Knife4j 启动崩溃
  datasource:                # MySQL: sky_take_out, root
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sky.entity      # 只有 entity 包有短别名
  configuration:
    map-underscore-to-camel-case: true
sky:
  jwt:
    admin-secret-key: sky-admin-secret-key  # 管理端密钥
    admin-ttl: 7200000
    admin-token-name: token
  alioss:                    # 阿里云 OSS（bucket: heicongqiong）
logging:
  level:
    com.sky: debug           # 打印 SQL，方便观察
```

> ⚠️ 后续做**用户端登录**时，需要新增 `sky.jwt.user-*`（用户端 JWT 配置，secret 与 admin 分开）。
> ⚠️ OSS 的 AccessKey 目前明文在 yml 里，属已知隐患，接手方不要外传。

---

## 八、剩余功能路线图（按黑马课程顺序）

> 每个模块给出：功能 / 接口 / 涉及表 / 设计要点。**接手方按此顺序推进**，每完成一个模块要求用户测试通过再进下一个。

### ① 用户端登录（微信登录·模拟）

- **接口**：`POST /user/user/login`
- **新增表**：`user`（id, openid 唯一, name, phone, sex, id_number, avatar, status, create_time, update_time）
- **设计要点**：
  - 模拟微信登录：前端传一个 `code`，后端**直接把 code 当 openid** 用（不真调微信接口）
  - 按 openid 查 user 表：不存在则自动注册新用户
  - 登录成功签发**用户端 JWT**（新增 `JwtProperties` 里 user 部分：`user-secret-key` / `user-ttl` / `user-token-name`）
  - 新增 `JwtTokenUserInterceptor`，拦截 `/user/**` 除 login；BaseContext 里存 userId
  - 新增 DTO/VO：`UserLoginDTO`(code)、`UserLoginVO`(id, openid, token)
  - 注意：现有 `WebMvcConfiguration` 里 admin 拦截器只拦 `/admin/**`，新增拦截器后 `/user/**` 也要校验（login 放行）

### ② 用户端分类 / 菜品 / 套餐浏览（引入 Redis 缓存）

- **接口**：`GET /user/category/list?type=`、`GET /user/dish/list?categoryId=`、`GET /user/setmeal/list?categoryId=`、`GET /user/dish/setmeal/{id}`（套餐下的菜品）
- **设计要点**：
  - 用户端只查**起售中**（status=1）的数据
  - 引入 `RedisTemplate` + **JSON 序列化配置**（StringRedisTemplate 只够存字符串，缓存对象列表需要配 Jackson 序列化器）
  - 缓存思路：key 如 `dish_分类id`，先查 Redis，没有则查库并写入，修改菜品时清缓存
  - 用户端接口要校验**店铺营业状态**（打烊时提示）

### ③ 购物车

- **接口**：`POST /user/shoppingCart/add`、`GET /user/shoppingCart/list`、`POST /user/shoppingCart/sub`、`DELETE /user/shoppingCart/clean`
- **新增表**：`shopping_cart`（id, name, image, user_id, dish_id, setmeal_id, dish_flavor, number, amount, create_time）
- **设计要点**：
  - 用 `BaseContext.getCurrentId()` 拿当前用户（此时用户端已接 JWT）
  - 同一菜品/套餐已存在则 **number+1**，否则新插入
  - 加购物车前校验菜品/套餐**是否起售**

### ④ 地址簿 + 用户下单

- **接口**：地址簿 `POST /user/addressBook/save`、`GET /user/addressBook/list`、`GET /user/addressBook/{id}`、`PUT /user/addressBook`、`DELETE /user/addressBook?id=`、`PUT /user/addressBook/default`、`GET /user/addressBook/default`；下单 `POST /user/order/submit`
- **新增表**：`address_book`（id, user_id, consignee, sex, phone, province_code, province_name, city_code, city_name, district_code, district_name, detail, label, is_default）、`orders`（id, number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status, amount, remark, phone, address, user_name, consignee, cancel_reason, rejection_reason, cancel_time, estimated_delivery_time, delivery_status, delivery_time, pack_amount, tableware_number, tableware_status）、`order_detail`（id, name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount）
- **设计要点**（下单是核心事务，重要）：
  - `@Transactional`：创建订单主表 + 批量插入 order_detail + 清空购物车，**一个事务**
  - 订单号：`System.currentTimeMillis() + 随机数` 生成唯一 `number`
  - 订单金额从购物车/菜品价格计算，**前端传的金额不可信**
  - 初始状态：待付款

### ⑤ 管理端订单管理（状态机）

- **接口**：`GET /admin/order/conditionSearch`（分页+条件）、`GET /admin/order/details/{id}`、`PUT /admin/order/confirm`（接单）、`PUT /admin/order/rejection`（拒单，带原因）、`PUT /admin/order/cancel`（取消，带原因）、`PUT /admin/order/delivery`（派送）、`PUT /admin/order/complete`（完成）
- **设计要点**：
  - 订单**状态机**：待付款(1) → 待接单(2) → 已接单(3) → 派送中(4) → 已完成(5)；另有已取消(6)、已拒单(7)
  - 拒单/取消必须写原因字段；每个状态变更同步更新对应时间字段

### ⑥ 用户支付（模拟）+ WebSocket 来单提醒

- **接口**：`PUT /user/order/payment`（模拟支付，参数为订单号）
- **设计要点**：
  - 模拟微信支付：不真调接口，直接改订单状态「待付款→待接单」、写 checkout_time、pay_status
  - 引入 **Spring WebSocket**：用户下单/支付后向管理端推送来单提醒；`@ServerEndpoint` 建立连接，管理端页面打开时建立 WS 连接接收
  - WebSocket 端点要加入 Spring 容器管理（配置类里注册）

### ⑦ 定时任务（Spring Task）

- **设计要点**：
  - 启动类加 `@EnableScheduling`
  - 定时清理**超时未支付订单**（如每 1 分钟扫一次，下单超过 15 分钟未支付则取消，恢复库存）
  - 定时**自动完成派送中订单**（营业时间结束后）
  - 新建 `task/` 包放定时任务类，用 `@Scheduled(cron=...)` 注解

### ⑧ 数据统计报表

- **接口**：`GET /admin/report/turnoverStatistics`（营业额）、`GET /admin/report/userStatistics`（用户统计）、`GET /admin/report/ordersStatistics`（订单统计）、`GET /admin/report/top10`（销量 Top10）、`GET /admin/report/export`（Excel 导出）
- **设计要点**：
  - 入参为日期区间 `begin/end`，返回日期的 `List` + 数据 `List`
  - 营业额 = 已完成订单金额合计；用户统计 = 新增用户/总用户；订单统计 = 订单数/有效订单/完成率；Top10 = 按销量聚合 order_detail
  - Excel 导出用 EasyExcel 或 POI（需加依赖），导出当日营业数据报表

---

## 九、对接手方的工作要求 ⚠️（最重要）

本项目是**教学项目**，用户是学生，通过「边讲边做」学习 Spring Boot。接手方必须：

1. **不代写功能代码**。每个模块：先讲概念、结构、思路，给设计要点和「思考题」，**让用户自己写**。
2. **审查用户写的代码**：逐文件读，指出问题并讲清「哪里错、为什么错」，**让用户自己改**。唯一例外：用户明确说「你替我改」时，可代改并说明改了哪些。
3. 写代码要**与现有代码风格完全一致**（见第四节约定），尤其：统一返回、异常处理、审计字段、先删后插、判空位置、resultType 全限定名、联表加别名。
4. 新模块开始前先提醒用户**补充对应的数据库表**（第八节每模块已列）。
5. 每完成一个模块，**给出一份测试清单**（Knife4j + curl + redis-cli），让用户实测通过再进下一个。
6. 环境相关的坑（见第五节）先自查，不要重复踩。
7. 涉及账号密钥（OSS AccessKey、JWT secret）注意不要外传；提醒用户测试后轮换。
8. 使用**简体中文**沟通。
