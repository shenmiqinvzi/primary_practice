# 苍穹外卖 · 前端任务书（Vue3 管理端 + 原生小程序）

> 本任务书指导**前端**两个工程的开发：**管理端**（Vue 3 + Element Plus）与**用户端**（原生微信小程序）。
> 前端对接已完成的后端接口（见 `任务书.md` 第六节接口清单与本文第五节接口对照表）。
> 教学方式与后端一致：**讲解思路 + 用户自己写 + 逐文件审查**，不代写功能代码。

---

## 一、项目概述

后端 8 个模块已全部完成（员工/分类/菜品/套餐/订单/支付/定时任务/报表），接口均可通过 curl 验证。
前端目标：把后端能力变成**可用的界面**。

```
管理端 /admin 页面（Vue 3 + Element Plus）      —— 餐厅老板/员工用
├── 登录页        员工账号登录，存 JWT
├── 工作台        今日数据概览 + 店铺营业状态开关
├── 员工管理      分页 / 新增 / 编辑 / 启用停用
├── 分类管理      分页 / 新增 / 修改 / 删除 / 启用停用
├── 菜品管理      分页 / 新增(含口味) / 批量删除 / 修改 / 启用停用
├── 套餐管理      分页 / 新增(关联菜品) / 批量删除 / 修改 / 启用停用
├── 订单管理      分页搜索 / 详情 / 接单 / 拒单 / 取消 / 派送 / 完成（WebSocket 来单提醒）
└── 数据统计      营业额/用户/订单/Top10 图表 + Excel 导出

用户端 /user 小程序（原生微信小程序）           —— 顾客点餐用
├── 登录          code 登录拿 JWT
├── 首页          店铺状态 + 分类 + 菜品/套餐
├── 菜品详情      看口味，加购物车
├── 购物车        增删改 / 清空
├── 下单          地址簿选择 / 提交订单
├── 支付          模拟支付
└── 订单          历史订单 / 详情 / 取消 / 催单
```

---

## 二、开发环境

| 组件 | 版本 / 位置 |
|---|---|
| Node.js | v24.19.0（已装） |
| npm | 11.17.0（已装） |
| 后端服务 | 本地 8080（`mvn spring-boot:run` 于 sky-server） |
| 微信开发者工具 | 需自行安装（小程序调试用） |
| 管理端前端脚手架 | Vite 5 + Vue 3 + Element Plus + Pinia + Vue Router + Axios |

### 常用命令

```bash
# 管理端：创建工程（在项目根目录）
npm create vite@latest sky-admin -- --template vue
cd sky-admin
npm install
npm run dev        # 默认 http://localhost:5173
```

> ⚠️ 小程序工程无法用 npm 创建，需在微信开发者工具里新建「小程序」项目，代码放在 `sky-miniapp/` 目录。

---

## 三、项目结构（前端）

```
苍穹外卖/
├── sky-admin/                 # 管理端（Vue 3 + Vite）
│   ├── index.html
│   ├── vite.config.js         # 配置 /api 代理 → http://localhost:8080
│   ├── src/
│   │   ├── main.js            # 挂载 app、router、pinia、element-plus
│   │   ├── App.vue
│   │   ├── router/index.js    # 路由 + 登录守卫
│   │   ├── stores/user.js     # Pinia：用户 token、登录状态
│   │   ├── api/               # axios 封装 + 各模块接口（employee/dish/order/...）
│   │   ├── utils/request.js   # axios 实例：baseURL、请求/响应拦截器
│   │   ├── views/
│   │   │   ├── Login.vue
│   │   │   ├── Layout.vue     # 左侧菜单 + 顶部 + <router-view>
│   │   │   ├── employee/…     # 员工/分类/菜品/套餐
│   │   │   ├── order/…        # 订单管理
│   │   │   └── report/…       # 数据统计
│   │   └── websocket/         # WebSocket 来单提醒
│   └── package.json
│
└── sky-miniapp/               # 用户端（原生微信小程序）
    ├── app.js                 # 全局逻辑（token、全局数据）
    ├── app.json               # 页面注册、tabBar
    ├── app.wxss               # 全局样式
    ├── utils/request.js       # wx.request 封装，统一带 token
    ├── pages/
    │   ├── login/             # 登录
    │   ├── index/             # 首页（分类 + 菜品列表）
    │   ├── dish/              # 菜品详情
    │   ├── cart/              # 购物车
    │   ├── address/           # 地址簿管理
    │   ├── submit/            # 确认下单页
    │   ├── pay/               # 支付页
    │   └── order/             # 历史订单 / 详情
    └── components/            # 自定义组件（菜品卡片等）
```

---

## 四、核心约定（前后端对接关键）

### 1. 接口地址与代理

- 后端接口统一前缀 `/admin/**`、`/user/**`
- 管理端：`vite.config.js` 配 dev 代理 `'/api' -> 'http://localhost:8080'`，**请求里不用写 `/api` 也行**（看你的配置）；生产环境直接把 baseURL 指向后端域名
- 小程序：`utils/request.js` 的 baseURL 直接填 `http://localhost:8080`（开发者工具需勾选「不校验合法域名」）

### 2. Token 传递（最关键的对接点）

| 端 | 请求头 | 来源 |
|---|---|---|
| 管理端 | `token` | 登录接口返回的 data 里的 token |
| 小程序 | `authentication` | 登录接口返回的 data 里的 token |

- 管理端：axios **请求拦截器**统一加 `headers['token'] = userStore.token`
- 小程序：`wx.request` 里手动加 `header: { authentication: token }`
- 登录成功 token 存 Pinia + `localStorage`（管理端）/ `wx.setStorageSync`（小程序）

### 3. 统一返回 `Result<T>`

```
{ code: 1, msg: null, data: ... }    // code=1 成功
{ code: 0, msg: "错误原因", data: null }  // 失败
```

- 响应拦截器：`res.data.code !== 1` 时 `ElMessage.error(res.data.msg)` 并 reject
- 失败统一 `code=0`，提示后端返回的 `msg`

### 4. 营业状态接口（ShopController）

`GET /user/shop/status`、`PUT /admin/shop/{status}`——管理端工作台开关 + 小程序首页展示

---

## 五、接口对照表（前端开发依据）

> 完整字段见 `任务书.md` 第六节。以下按前端页面归类，**括号内为后端路径**。

### 管理端
| 功能 | 接口 | 方法 |
|---|---|---|
| 登录 | `/admin/employee/login` | POST |
| 员工分页 | `/admin/employee/page` | GET |
| 员工增改删启停 | `/admin/employee` / `/{id}` / `/status/{status}` | POST/PUT/DELETE |
| 分类分页 | `/admin/category/page` | GET |
| 分类增改删启停/按类型 | `/admin/category` / `/status/{status}` | POST/PUT/DELETE/GET |
| 菜品分页 | `/admin/dish/page` | GET |
| 菜品增改删启停/按分类 | `/admin/dish` / `/status/{status}` | POST/PUT/DELETE/GET |
| 套餐分页 | `/admin/setmeal/page` | GET |
| 套餐增改删启停/回显 | `/admin/setmeal` / `/status/{status}` / `/{id}` | POST/PUT/DELETE/GET |
| 上传图片 | `/admin/common/upload` | POST |
| 营业状态 | `/admin/shop/{status}` | PUT |
| 订单分页搜索 | `/admin/order/conditionSearch` | GET |
| 订单详情/统计 | `/admin/order/details/{id}` / `/statistics` | GET |
| 接单/拒单/取消 | `/admin/order/confirm|rejection|cancel` | PUT |
| 派送/完成 | `/admin/order/delivery/{id}` / `complete/{id}` | PUT |
| 报表 | `/admin/report/turnoverStatistics` 等 4 个 | GET |
| Excel 导出 | `/admin/report/export` | GET（下载） |
| WebSocket | `ws://localhost:8080/ws/{sid}` | — |

### 用户端小程序
| 功能 | 接口 | 方法 |
|---|---|---|
| 登录 | `/user/user/login` | POST |
| 营业状态 | `/user/shop/status` | GET |
| 分类 | `/user/category/list?type=` | GET |
| 菜品 | `/user/dish/list?categoryId=` | GET |
| 套餐 | `/user/setmeal/list?categoryId=`、`/user/dish/setmeal/{id}` | GET |
| 购物车 | `/user/shoppingCart/add|list|sub|clean` | POST/GET/POST/DELETE |
| 地址簿 | `/user/addressBook/save|list|...|default` | POST/GET/.../PUT |
| 下单 | `/user/order/submit` | POST |
| 支付 | `/user/order/payment` | PUT |
| 历史订单 | `/user/order/historyOrders` | GET |
| 订单详情 | `/user/order/orderDetail/{id}` | GET |
| 取消订单 | `/user/order/cancel` | PUT |
| 催单 | `/user/order/reminder/{id}` | GET |

---

## 六、功能路线图（按黑马课程顺序）

### 管理端（Vue 3）

#### ① 脚手架 + 登录 + 布局
- 用 Vite 创建项目，装 `element-plus` `pinia` `vue-router` `axios`
- 登录页：表单（username/password）→ 调登录接口 → 存 token → 跳工作台
- 路由守卫：无 token 跳 `/login`
- Layout：侧边菜单 + 顶部栏 + 内容区
- **思考**：axios 拦截器在哪一层统一加 token？401 时怎么办？

#### ② 工作台 + 店铺营业状态
- 工作台展示今日营业额/订单数/有效订单/新增用户（调报表接口）
- 营业状态开关（调 `/admin/shop/{status}`）
- **思考**：开关是「开→关」还是「关→开」？后端参数 1/0 对应什么？

#### ③ 员工管理
- 表格分页 + 搜索（name）
- 新增/编辑对话框、启用停用 switch
- **思考**：新增员工密码后端怎么处理（MD5）？前端要不要管？

#### ④ 分类管理
- 分页表格 + 类型筛选（菜品/套餐）
- 新增/编辑/删除、启用停用
- **注意**：分类的 name 唯一，重复提交后端会报错——前端要不要提前校验？

#### ⑤ 菜品管理（最复杂）
- 分页 + 按分类筛选（级联下拉：先选分类）
- 新增/编辑：菜品信息 + **口味动态增删行**（dishFlavor 数组）
- 批量删除、启用停用、图片上传（调 `/admin/common/upload` 拿 URL）
- **思考**：口味是 `[{name,value}]`，编辑时如何回显到表单？新增和编辑共用一个弹窗组件怎么处理数据差异？

#### ⑥ 套餐管理
- 分页 + 新增（**选菜品**：从菜品列表多选，回显名称/价格/份数）
- 回显详情（`/admin/setmeal/{id}` 返回关联菜品列表）
- **思考**：套餐里的「菜品列表」后端存在 setmeal_dish 表，编辑时怎么提交这份关联数据？

#### ⑦ 订单管理 + WebSocket 来单提醒
- 条件搜索：订单号/手机号/状态/时间
- 详情抽屉：订单信息 + 明细列表 + 状态操作按钮（按当前状态显示可操作项）
- 接单/拒单/取消（填原因）/派送/完成
- WebSocket：连接 `/ws/1`，收「来单提醒」，弹 `ElMessage` + 刷新列表
- **思考**：WebSocket 消息有「来单提醒(type=1)」和「催单(type=2)」，前端怎么区分处理？

#### ⑧ 数据统计 + Excel 导出
- 日期范围选择（el-date-picker，默认近7天/近30天）
- 营业额/用户/订单：折线图（可用 ECharts 或简化为表格）
- Top10：横向条形
- Excel 导出：`window.open('/api/admin/report/export?...')` 或 a 标签下载
- **思考**：导出接口返回的是文件流，axios 直接请求会拿到二进制，怎么处理下载？最简单的方式是什么？

### 用户端小程序（原生）

#### ⑨ 登录 + 首页
- `wx.login` 拿 code → `/user/user/login` 拿 token 存 storage
- 首页：营业状态 + 分类 tab + 菜品列表（swiper 或 scroll-view）
- **思考**：小程序没有浏览器 localStorage，token 存在哪？请求怎么统一带上？

#### ⑩ 购物车 + 下单 + 支付
- 购物车页面：列表/加减/清空
- 下单：选地址（地址簿列表）→ 提交 → 跳支付页
- 支付：调 `/user/order/payment` → 成功提示
- **思考**：下单接口一次调用，后端就建了订单并清空购物车。如果支付失败怎么办？（后端订单状态还是待付款，可取消）

#### ⑪ 订单 + 催单
- 历史订单列表（tab 按状态）
- 订单详情、待付款可取消、催单按钮
- **思考**：催单成功提示什么？后端推 JSON `{type:2,...}`，这个提醒到哪去了？（管理端 WebSocket）

---

## 七、已踩过的坑（前端）

1. **CORS / 跨域**：Vite dev 用 `server.proxy` 代理，别直接跨域调 8080
2. **Token 头名**：管理端 `token`、小程序 `authentication`，别带错
3. **Element Plus 组件按需引入**：Vite 装 `unplugin-vue-components` + `unplugin-auto-import` 更省事；或全量引入最简单（教学推荐全量）
4. **图片上传**：OSS 直传 vs 后端中转——本项目后端已封装 `/admin/common/upload`，前端直接调即可，返回 `{url}` 填入表单 image 字段
5. **小程序 request**：`wx.request` 是异步的，封装 Promise；`success` 里看 `res.data.code`
6. **日期处理**：报表接口 `begin/end` 格式 `yyyy-MM-dd`，用 dayjs 或手写格式化

---

## 八、对接手方的工作要求 ⚠️

1. **不代写前端功能代码**。每页先讲布局结构、用到哪些组件、接口怎么调、数据结构长啥样，**让用户自己写**。
2. **逐文件审查**：读用户的 .vue/.js/.wxml，指出问题讲清「为什么」，让用户自己改。
3. 与后端约定的对接点（token 头、Result 结构、字段名）必须一致，先自查接口对照表。
4. 每完成一个页面，**给出一份验证清单**（浏览器操作 / 微信开发者工具操作 + 后端日志确认），实测通过再进下一个。
5. 后端已有待办（管理端清 Redis 缓存、OSS 密钥轮换）暂不影响前端，提醒用户留意。
6. 使用**简体中文**沟通。