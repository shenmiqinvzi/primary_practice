<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 必须是 Mapper 接口的全限定名，MyBatis 才能把它们对应起来 -->
<mapper namespace="com.sky.mapper.EmployeeMapper">

    <!-- id 必须等于接口方法名；resultType 用实体类名（配置了 type-aliases-package） -->
    <select id="getByUsername" resultType="Employee">
        select * from employee
        where username = #{username}
    </select>

    <select id="pageQuery" resultType="com.sky.entity.Employee">
        select id, username, name, password, phone, sex, id_number, status, create_time, update_time, create_user, update_user
        from employee
        <where>
            <!-- name不为空、不为空串时，拼接模糊查询条件 -->
            <if test="name != null and name != ''">
                name like concat('%', #{name}, '%')
            </if>
        </where>
        order by create_time desc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into employee(name,username,password,phone,sex,id_number,status,create_time,update_time,create_user,update_user)
        values (#{name}, #{username}, #{password}, #{phone}, #{sex},
        #{idNumber}, #{status}, #{createTime}, #{updateTime},
        #{createUser}, #{updateUser})
    </insert>

    <update id="update">
        update employee
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="idNumber != null">id_number = #{idNumber},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createUser != null">create_user = #{createUser},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        </set>
        where id=#{id}
    </update>

    <select id="getById" resultType="com.sky.entity.Employee">
        select * from employee where id=#{id}
    </select>

</mapper>
